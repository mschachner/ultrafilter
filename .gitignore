node_modules/
.DS_Store
# Build output — generated by the workflow (or locally); never committed.
data/
# A local checkout of the `data` branch (git worktree add store data), which
# the build reads when present. In Actions the workflow checks it out here.
store/
